DEF[PORTVERSION]=	0.20
# ------------------------------------------------------------------------

NAMEBASE=		perl-Pod-Abstract
VERSION=		${PORTVERSION}
KEYWORDS=		perl
VARIANTS=		532 534
SDESC[532]=		Abstract document tree for POD documents (5.32)
SDESC[534]=		Abstract document tree for POD documents (5.34)
HOMEPAGE=		none
CONTACT=		Perl_Automaton[perl@ironwolf.systems]

DOWNLOAD_GROUPS=	main
SITES[main]=		CPAN/ID:B/BL/BLILBURNE
DISTFILE[1]=		Pod-Abstract-${PORTVERSION}.tar.gz:main

SPKGS[532]=		single
SPKGS[534]=		single

OPTIONS_AVAILABLE=	PERL_532 PERL_534
OPTIONS_STANDARD=	none
VOPTS[532]=		PERL_532=ON PERL_534=OFF
VOPTS[534]=		PERL_532=OFF PERL_534=ON

# License listed in CPAN directory:
# Not provided in META.yml

GENERATED=		yes
SINGLE_JOB=		yes
DISTNAME=		Pod-Abstract-${PORTVERSION}

# ---------------------------------------------
# |   Prerequisites extracted from META.yml
# ---------------------------------------------
# |           requires : File::Temp (perl 5.32.1 core) (perl 5.34.0 core)
# |           requires : IO::String
# |           requires : Pod::Parser
# |           requires : Scalar::Util (perl 5.32.1 core) (perl 5.34.0 core)
# |           requires : Task::Weaken
# |           requires : Test::More (perl 5.32.1 core) (perl 5.34.0 core)

[PERL_532].USES_ON=			perl:532,configure
[PERL_532].BUILDRUN_DEPENDS_ON=		perl-IO-String:single:532
					perl-Pod-Parser:single:532
					perl-Task-Weaken:single:532
[PERL_534].USES_ON=			perl:534,configure
[PERL_534].BUILDRUN_DEPENDS_ON=		perl-IO-String:single:534
					perl-Pod-Parser:single:534
					perl-Task-Weaken:single:534

