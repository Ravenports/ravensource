--- meson.build.orig	2026-06-16 12:01:17 UTC
+++ meson.build
@@ -1342,6 +1342,7 @@ if opt and not is_disabler(exe)
   bashcompletions += ['rev']
 endif
 
+if false
 exe = executable(
   'column',
   column_sources,
@@ -1356,6 +1357,7 @@ if not is_disabler(exe)
   manadocs += column_manadocs
   bashcompletions += ['column']
 endif
+endif
 
 opt = not get_option('build-line').disabled()
 exe = executable(
@@ -1668,6 +1670,7 @@ if opt and not is_disabler(exe)
   bashcompletions += ['dmesg']
 endif
 
+if false
 exe = executable(
   'test_dmesg',
   dmesg_sources,
@@ -1679,6 +1682,7 @@ exe = executable(
 if opt and not is_disabler(exe)
   exes += exe
 endif
+endif
 
 opt = get_option('build-ctrlaltdel').allowed()
 exe = executable(
@@ -2705,6 +2709,7 @@ if opt and not is_disabler(exe)
   bashcompletions += ['scriptlive']
 endif
 
+if false
 exe = executable(
   'scriptreplay',
   scriptreplay_sources,
@@ -2719,6 +2724,7 @@ if opt and not is_disabler(exe)
   manadocs += scriptreplay_manadocs
   bashcompletions += ['scriptreplay']
 endif
+endif
 
 opt = not get_option('build-agetty').disabled()
 exe = executable(
@@ -3306,6 +3312,7 @@ if opt and not is_disabler(exe)
   bashcompletions += ['pipesz']
 endif
 
+if false
 exe = executable(
   'test_cal',
   cal_sources,
@@ -3318,6 +3325,7 @@ exe = executable(
 if not is_disabler(exe)
   exes += exe
 endif
+endif
 
 have_posix_fadvise = conf.get('HAVE_POSIX_FADVISE').to_string() == '1'
 
@@ -3536,6 +3544,7 @@ if not is_disabler(exe)
   exes += exe
 endif
 
+if false
 exe = executable(
   'test_colors',
   'lib/colors.c',
@@ -3547,6 +3556,7 @@ exe = executable(
 if not is_disabler(exe)
   exes += exe
 endif
+endif
 
 exe = executable(
   'test_randutils',
