DEF[LIBVERSION]=	13.0.70
DEF[PORTVERSION]=	0.13.70
DEF[LIBS]=		zzip zzipfseeko zzipmmapped zzipwrap
# 0.13.70 is DOA, use later tag
# DEF[TAG]=		v${PORTVERSION}
DEF[TAG]=		3d55706
# ----------------------------------------------------------------------------

NAMEBASE=		zziplib
VERSION=		${PORTVERSION}
KEYWORDS=		devel
VARIANTS=		standard
SDESC[standard]=	Provide transparent read access to zipped data
HOMEPAGE=		https://github.com/gdraheim/zziplib
CONTACT=		Michael_Reim[kraileth@elderlinux.org]

DOWNLOAD_GROUPS=	main
SITES[main]=		GITHUB/gdraheim:zziplib:${TAG}
DISTFILE[1]=		generated:main

SPKGS[standard]=	complete primary sdl

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

LICENSE=		LGPL20:primary
			MPL:primary
LICENSE_FILE=		LGPL20:{{WRKSRC}}/docs/COPYING.LIB
			MPL:{{WRKSRC}}/docs/COPYING.MPL
LICENSE_TERMS=		primary:{{WRKDIR}}/TERMS
LICENSE_AWK=		TERMS:"^$$"
LICENSE_SOURCE=		TERMS:{{WRKSRC}}/zzip/zzip.h
LICENSE_SCHEME=		dual

FPC_EQUIVALENT=		devel/zziplib

USES=			shebangfix pkgconfig zlib cmake
			python:build
SDL_COMPONENTS=		sdl2
SHEBANG_FILES=		docs/*.py
BUILD_DEPENDS=		sdl1:primary:standard
			xmlto:single:standard
			zip:single:standard
			bash:primary:standard
# #			BUILD_STATIC_LIBS isn't accepted (why?)
CMAKE_ARGS=		-DBUILD_SHARED_LIBS:BOOL=ON
			-DZZIPSDL:BOOL=ON
INSTALL_REQ_TOOLCHAIN=	yes

EXRUN[sdl]=		sdl1:primary:standard
SOVERSION=		${LIBVERSION}

post-patch:
	# disable docs
	${REINPLACE_CMD} -e '/SUBDIRS =/ s|test docs||' ${WRKSRC}/Makefile

post-install:
.for l in ${LIBS}
	${STRIP_CMD} ${STAGEDIR}${PREFIX}/lib/lib${l}-0.so.${LIBVERSION}
.endfor
