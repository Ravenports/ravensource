DEF[PORTVERSION]=	4.10.0
DEF[FOLDER]=		91/ff/2c0bab6e1fcd837c52afd99a37e0cc16f2e1c970d313c3d4bc10b5c047f2
# ------------------------------------------------------------------------

NAMEBASE=		scons
VERSION=		${PORTVERSION}
KEYWORDS=		devel python
VARIANTS=		std
SDESC[std]=		Alternative build tool competing with make
HOMEPAGE=		https://www.scons.org/
CONTACT=		nobody

DOWNLOAD_GROUPS=	main
SITES[main]=		PYPIWHL/${FOLDER}
DISTFILE[1]=		scons-${PORTVERSION}.tar.gz:main

SPKGS[std]=		single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

LICENSE=		MIT:single
LICENSE_FILE=		MIT:{{WRKSRC}}/LICENSE
LICENSE_SCHEME=		solo

FPC_EQUIVALENT=		devel/scons

RUN_DEPENDS=		m4:primary:std

USES=			python:sutools

post-patch:
	${FIND} ${WRKSRC} -name \*.orig -delete
