DEF[PORTVERSION]=	4.10.1
DEF[FOLDER]=		7d/c9/2f430bb39e4eccba32ce8008df4a3206df651276422204e177a09e12b30b
# ------------------------------------------------------------------------

NAMEBASE=		scons
VERSION=		${PORTVERSION}
KEYWORDS=		devel python
VARIANTS=		std
SDESC[std]=		Alternative build tool competing with make
HOMEPAGE=		https://www.scons.org/
CONTACT=		nobody

DOWNLOAD_GROUPS=	main
SITES[main]=		PYPIWHL/${FOLDER}
DISTFILE[1]=		scons-${PORTVERSION}.tar.gz:main

SPKGS[std]=		single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

LICENSE=		MIT:single
LICENSE_FILE=		MIT:{{WRKSRC}}/LICENSE
LICENSE_SCHEME=		solo

FPC_EQUIVALENT=		devel/scons

RUN_DEPENDS=		m4:primary:std

USES=			python:sutools

post-patch:
	${FIND} ${WRKSRC} -name \*.orig -delete
