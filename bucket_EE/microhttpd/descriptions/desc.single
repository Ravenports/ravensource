micro_httpd is a very small Unix-based HTTP server. It runs from inetd, which means performance is poor. But for low-traffic sites, it's quite adequate. It implement all the basic features of an HTTP server.