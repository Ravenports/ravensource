--- Makerules.orig	2026-02-18 16:24:19 UTC
+++ Makerules
@@ -108,6 +108,9 @@ endif
 ifeq ($(OS),Darwin)
   LDREMOVEUNREACH := -Wl,-dead_strip
   SO := dylib
+else ifeq ($(OS),SunOS)
+  LDREMOVEUNREACH := -Wl,-z,ignore
+  SO := so
 else
   LDREMOVEUNREACH := -Wl,--gc-sections
   SO := so
@@ -236,7 +239,7 @@ ifeq ($(OS),Darwin)
 
 else
 
-  ifeq ($(OS),Linux)
+  ifeq ($(OS),%%OPSYS%%)
     HAVE_OBJCOPY := yes
   endif
 
@@ -331,7 +334,7 @@ else
   endif
 
   ifeq ($(HAVE_LIBCRYPTO),)
-    HAVE_LIBCRYPTO := $(shell pkg-config --exists 'libcrypto >= 1.1.0' && echo yes)
+    HAVE_LIBCRYPTO := no
   endif
   ifeq ($(HAVE_LIBCRYPTO),yes)
     LIBCRYPTO_CFLAGS := $(shell pkg-config --cflags libcrypto) -DHAVE_LIBCRYPTO
