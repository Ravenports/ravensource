DEF[PORTVERSION]=	20210218
# ----------------------------------------------------------------------------

NAMEBASE=		xorg-luit
VERSION=		${PORTVERSION}
KEYWORDS=		x11
VARIANTS=		standard
SDESC[standard]=	Locale and ISO 2022 support for Unicode terminals
HOMEPAGE=		https://invisible-island.net/luit/
CONTACT=		nobody

DOWNLOAD_GROUPS=	main
SITES[main]=		http://download.openpkg.org/components/cache/luit/
			ftp://ftp.invisible-island.net/luit/
DISTFILE[1]=		luit-${PORTVERSION}.tgz:main

SPKGS[standard]=	single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

LICENSE=		MIT:single
LICENSE_FILE=		MIT:{{WRKSRC}}/COPYING
LICENSE_SCHEME=		solo

FPC_EQUIVALENT=		x11/luit

BUILD_DEPENDS=		xorg-encodings:single:standard

USES=			iconv pkgconfig
XORG_COMPONENTS=	ice xt
DISTNAME=		luit-${PORTVERSION}
MUST_CONFIGURE=		gnu
CONFIGURE_ARGS=		--x-libraries={{LOCALBASE}}/lib
			--x-includes={{LOCALBASE}}/include
			--with-encodings-dir={{LOCALBASE}}/share/fonts/encodings/encodings.dir
			--with-locale-alias={{LOCALBASE}}/lib/X11/locale/locale.alias
			--with-pkg-config={{LOCALBASE}}/bin/pkg-config
