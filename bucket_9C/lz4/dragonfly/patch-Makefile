--- Makefile.orig	2017-04-01 00:45:09 UTC
+++ Makefile
@@ -87,7 +87,7 @@ clean:
 #------------------------------------------------------------------------
 #make install is validated only for Linux, OSX, kFreeBSD, Hurd and
 #FreeBSD targets
-ifneq (,$(filter $(shell uname),Linux Darwin GNU/kFreeBSD GNU FreeBSD))
+ifneq (,$(filter $(shell uname),Linux Darwin GNU/kFreeBSD GNU DragonFly FreeBSD))
 HOST_OS = POSIX
 
 install:
