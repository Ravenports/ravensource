DEF[PORTVERSION]=	26.1.0
# ------------------------------------------------------------------------

NAMEBASE=		python-gunicorn
VERSION=		${PORTVERSION}
KEYWORDS=		python
VARIANTS=		v13 v14
SDESC[v13]=		WSGI HTTP Server for UNIX (3.13)
SDESC[v14]=		WSGI HTTP Server for UNIX (3.14)
HOMEPAGE=		https://gunicorn.org
CONTACT=		Python_Automaton[python@ravenports.com]

DOWNLOAD_GROUPS=	main
SITES[main]=		PYPIWHL/19/dc/7a55fc605543fd5cb11c003fbbb21a1911d5e88a582cce6c5e063bf5c176
DISTFILE[1]=		gunicorn-${PORTVERSION}-py3-none-any.whl:main
DIST_SUBDIR=		python-src

SPKGS[v13]=		single
SPKGS[v14]=		single

OPTIONS_AVAILABLE=	PY313 PY314
OPTIONS_STANDARD=	none
VOPTS[v13]=		PY313=ON PY314=OFF
VOPTS[v14]=		PY313=OFF PY314=ON

# License listed at PyPI
# Not provided.

GENERATED=		yes
DISTNAME=		gunicorn-${PORTVERSION}.dist-info

# Requires-Dist extracted from wheel metadata
# gevent>=24.10.1; extra == "gevent"
# packaging; extra == "gevent"
# tornado>=6.5.7; extra == "tornado"
# setproctitle; extra == "setproctitle"
# h2>=4.4.1; extra == "http2"
# gunicorn_h1c>=0.6.6; extra == "fast"
# gevent>=24.10.1; extra == "testing"
# h2>=4.4.1; extra == "testing"
# coverage; extra == "testing"
# packaging; extra == "testing"
# pytest>=9.0.3; extra == "testing"
# pytest-cov; extra == "testing"
# pytest-asyncio; extra == "testing"
# uvloop>=0.19.0; extra == "testing"
# httpx[http2]>=0.23.0; extra == "testing"
# inotify>=0.2.10; sys_platform == "linux" and extra == "testing"

[PY313].USES_ON=			python:v13,wheel
[PY314].USES_ON=			python:v14,wheel

USES=			cpe
CPE_VENDOR=		gunicorn
CPE_PRODUCT=		gunicorn
