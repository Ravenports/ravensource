primary: {
  type: "install"
  message: <<EOM
During SMF testing, the server comes up on OmniOS but hits a fault that
emits a stacktrace on a freshly initialized database.

This version of mysql may require additional patches for SunOS.
You may prefer to uninstall this version and move to the mysql~server
package instead, which does stay online.
EOM
}
