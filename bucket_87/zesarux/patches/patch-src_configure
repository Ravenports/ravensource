--- configure.orig	2026-06-09 16:37:08 UTC
+++ configure
@@ -717,7 +717,7 @@ if [ -n "$OPTIONOBJCURSES" ]; then
 	TEMPFILE="configure.compile.c"
 
 	cat > $TEMPFILE << _EOF
-#include <curses.h>
+#include <ncurses/curses.h>
 int main () {
 return 0;
 }
