--- configure.orig	2025-09-25 16:14:00 UTC
+++ configure
@@ -689,7 +689,7 @@ if [ "$OPTIONOBJCURSES" != "" ]; then
 	TEMPFILE="configure.compile.c"
 
 	cat > $TEMPFILE << _EOF
-#include <curses.h>
+#include <ncurses/curses.h>
 int main () {
 return 0;
 }
