--- configure.orig	2025-01-16 17:27:12 UTC
+++ configure
@@ -689,7 +689,7 @@ if [ "$OPTIONOBJCURSES" != "" ]; then
 	TEMPFILE="configure.compile.c"
 
 	cat > $TEMPFILE << _EOF
-#include <curses.h>
+#include <ncurses/curses.h>
 int main () {
 return 0;
 }
