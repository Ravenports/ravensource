--- pyproject.toml.orig	2026-09-02 10:24:22 UTC
+++ pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["Cython>=3.2.9,<3.3", "setuptools"]
+requires = ["setuptools"]
 
 [tool.cibuildwheel]
 build-verbosity = 1
