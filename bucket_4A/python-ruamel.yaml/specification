DEF[PORTVERSION]=	0.16.7
# ------------------------------------------------------------------------

NAMEBASE=		python-ruamel.yaml
VERSION=		${PORTVERSION}
KEYWORDS=		python devel textproc
VARIANTS=		py38 py37
SDESC[py38]=		YAML 1.2 parser/emitter (PY 38)
SDESC[py37]=		YAML 1.2 parser/emitter (PY 37)
HOMEPAGE=		https://bitbucket.org/ruamel/yaml
CONTACT=		Python_Automaton[python@ironwolf.systems]

DOWNLOAD_GROUPS=	main
SITES[main]=		PYPI/r/ruamel.yaml
DISTFILE[1]=		ruamel.yaml-0.16.7.tar.gz:main

SPKGS[py38]=		single
SPKGS[py37]=		single

OPTIONS_AVAILABLE=	PY38 PY37
OPTIONS_STANDARD=	none
VOPTS[py38]=		PY38=ON PY37=OFF
VOPTS[py37]=		PY38=OFF PY37=ON

# Output of setup.py --license
# MIT license

GENERATED=		yes

# install_requires extracted from setup.py (using python 2.7)

DISTNAME=		ruamel.yaml-${PORTVERSION}

[PY38].USES_ON=				python:py38

[PY37].USES_ON=				python:py37

