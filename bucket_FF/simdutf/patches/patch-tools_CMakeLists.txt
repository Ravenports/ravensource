--- tools/CMakeLists.txt.orig	2026-04-21 21:01:59 UTC
+++ tools/CMakeLists.txt
@@ -59,7 +59,7 @@ if((CMAKE_CXX_COMPILER_ID STREQUAL "GNU"
   else()
     message(STATUS "libstdc++.a not found")
   endif()
-  if(NOT APPLE)
+  if(NOT APPLE AND NOT ${CMAKE_SYSTEM_NAME} MATCHES "SunOS")
     target_link_options(sutf PRIVATE "-Wl,--gc-sections")
     target_link_options(fastbase64 PRIVATE "-Wl,--gc-sections")
   endif()
