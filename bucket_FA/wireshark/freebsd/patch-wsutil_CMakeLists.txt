--- wsutil/CMakeLists.txt.orig	2019-12-04 18:35:05 UTC
+++ wsutil/CMakeLists.txt
@@ -268,6 +268,7 @@ target_link_libraries(wsutil
 	${GCRYPT_LIBRARIES}
 	${WIN_WSOCK32_LIBRARY}
 	${GNUTLS_LIBRARIES}
+	-lm
 )
 
 if(WIN32)
