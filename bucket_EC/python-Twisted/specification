DEF[PORTVERSION]=	20.3.0
# ------------------------------------------------------------------------

NAMEBASE=		python-Twisted
VERSION=		${PORTVERSION}
KEYWORDS=		python
VARIANTS=		py38 py37
SDESC[py38]=		Asynchronous networking framework (PY38)
SDESC[py37]=		Asynchronous networking framework (PY37)
HOMEPAGE=		https://twistedmatrix.com/
CONTACT=		Python_Automaton[python@ironwolf.systems]

DOWNLOAD_GROUPS=	main
SITES[main]=		PYPI/T/Twisted
DISTFILE[1]=		Twisted-${PORTVERSION}.tar.bz2:main

SPKGS[py38]=		single
SPKGS[py37]=		single

OPTIONS_AVAILABLE=	PY38 PY37
OPTIONS_STANDARD=	none
VOPTS[py38]=		PY38=ON PY37=OFF
VOPTS[py37]=		PY38=OFF PY37=ON

# License listed at PyPI
# MIT

# install_requires extracted from setup.py


DISTNAME=		Twisted-${PORTVERSION}
GENERATED=		yes

[PY38].USES_ON=				python:py38
[PY37].USES_ON=				python:py37

BUILD_DEPENDS=		python-incremental:single:python_used

USES=			cpe
CPE_VENDOR=		twistedmatrix
CPE_PRODUCT=		twisted
