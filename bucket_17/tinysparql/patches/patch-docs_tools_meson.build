--- docs/tools/meson.build.orig	2026-04-14 21:01:56 UTC
+++ docs/tools/meson.build
@@ -14,5 +14,5 @@ tracker_docgen = executable('tracker-doc
     doctool_gresources,
     # Prefer RPATH over RUNPATH so the executable
     # always finds the uninstalled library.
-    link_args: ['-Wl,--disable-new-dtags'],
+    link_args: [],
     dependencies: [tracker_sparql_dep])
