DEF[PORTVERSION]=	0.01
# ------------------------------------------------------------------------

NAMEBASE=		perl-Algorithm-SpatialIndex-Storage-Redis
VERSION=		${PORTVERSION}
KEYWORDS=		perl
VARIANTS=		542 544
SDESC[542]=		Redis storage backend (5.42)
SDESC[544]=		Redis storage backend (5.44)
HOMEPAGE=		none
CONTACT=		Perl_Automaton[perl@ravenports.com]

DOWNLOAD_GROUPS=	main
SITES[main]=		CPAN/ID:S/SM/SMUELLER
DISTFILE[1]=		Algorithm-SpatialIndex-Storage-Redis-${PORTVERSION}.tar.gz:main

SPKGS[542]=		single
SPKGS[544]=		single

OPTIONS_AVAILABLE=	PERL_542 PERL_544
OPTIONS_STANDARD=	none
VOPTS[542]=		PERL_542=ON PERL_544=OFF
VOPTS[544]=		PERL_542=OFF PERL_544=ON

# License listed in CPAN directory:
# unknown

GENERATED=		yes
SINGLE_JOB=		yes
DISTNAME=		Algorithm-SpatialIndex-Storage-Redis-${PORTVERSION}

# ----------------------------------------------
# |   Prerequisites extracted from META.json
# ----------------------------------------------
# |      runtime : Algorithm::SpatialIndex
# |      runtime : JSON
# |      runtime : Redis
# |      runtime : Scalar::Util (perl 5.42 core) (perl 5.44 core)
# |      runtime : Sereal::Decoder
# |      runtime : Sereal::Encoder
# |        build : ExtUtils::MakeMaker (perl 5.42 core) (perl 5.44 core)
# |    configure : ExtUtils::MakeMaker (perl 5.42 core) (perl 5.44 core)

[PERL_542].USES_ON=			perl:542,configure
[PERL_542].BUILDRUN_DEPENDS_ON=		perl-Algorithm-SpatialIndex:single:542
					perl-JSON:single:542
					perl-Redis:single:542
					perl-Sereal-Decoder:single:542
					perl-Sereal-Encoder:single:542
[PERL_544].USES_ON=			perl:544,configure
[PERL_544].BUILDRUN_DEPENDS_ON=		perl-Algorithm-SpatialIndex:single:544
					perl-JSON:single:544
					perl-Redis:single:544
					perl-Sereal-Decoder:single:544
					perl-Sereal-Encoder:single:544

