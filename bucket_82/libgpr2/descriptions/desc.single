LibGPR2 - Parser for GPR Project files

The tools conflict with the tools in gprbuild, so the GPR2 tools may
essentially be a replacement for gprbuild.  However, to avoid conflict,
they've been relocated to /raven/gpr2/bin so both packages can coexist.
