--- CMakeLists.txt.orig	2024-04-19 21:00:35 UTC
+++ CMakeLists.txt
@@ -926,6 +926,7 @@ set(ZIG_BUILD_ARGS
   "-Dcpu=${ZIG_TARGET_MCPU}"
   ${ZIG_DYNAMIC_LINKER_ARG}
   "-Dversion-string=${RESOLVED_ZIG_VERSION}"
+  "--verbose"
 )
 
 add_custom_target(stage3 ALL
