DEF[PORTVERSION]=	0.1.2
# ------------------------------------------------------------------------

NAMEBASE=		R-alphavantager
VERSION=		${PORTVERSION}
KEYWORDS=		cran
VARIANTS=		standard
SDESC[standard]=	Lightweight R Interface to the Alpha Vantage API
HOMEPAGE=		https://github.com/business-science/alphavantager
CONTACT=		CRAN_Automaton[cran@ironwolf.systems]

DOWNLOAD_GROUPS=	main
SITES[main]=		CRAN/src/contrib
DISTFILE[1]=		alphavantager_0.1.2.tar.gz:main
DIST_SUBDIR=		CRAN
DF_INDEX=		1

SPKGS[standard]=	single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

# License listed on https://cran.r-project.org/
# => GPL (>= 3)

USES=			cran gmake
DISTNAME=		alphavantager
GENERATED=		yes
INSTALL_REQ_TOOLCHAIN=	yes

BUILDRUN_DEPENDS=	R-dplyr:single:standard
			R-glue:single:standard
			R-httr:single:standard
			R-jsonlite:single:standard
			R-purrr:single:standard
			R-readr:single:standard
			R-stringr:single:standard
			R-tibble:single:standard
			R-tidyr:single:standard
			R-timetk:single:standard


