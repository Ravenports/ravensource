DEF[PORTVERSION]=	6.01
# ------------------------------------------------------------------------

NAMEBASE=		perl-HTTP-Negotiate
VERSION=		${PORTVERSION}
KEYWORDS=		perl
VARIANTS=		542 544
SDESC[542]=		Choose a variant to serve (5.42)
SDESC[544]=		Choose a variant to serve (5.44)
HOMEPAGE=		https://github.com/gisle/http-negotiate
CONTACT=		Perl_Automaton[perl@ravenports.com]

DOWNLOAD_GROUPS=	main
SITES[main]=		CPAN/ID:G/GA/GAAS
DISTFILE[1]=		HTTP-Negotiate-${PORTVERSION}.tar.gz:main

SPKGS[542]=		single
SPKGS[544]=		single

OPTIONS_AVAILABLE=	PERL_542 PERL_544
OPTIONS_STANDARD=	none
VOPTS[542]=		PERL_542=ON PERL_544=OFF
VOPTS[544]=		PERL_542=OFF PERL_544=ON

# License listed in CPAN directory:
# perl

GENERATED=		yes
SINGLE_JOB=		yes
DISTNAME=		HTTP-Negotiate-${PORTVERSION}

# ---------------------------------------------
# |   Prerequisites extracted from META.yml
# ---------------------------------------------
# | configure_requires : ExtUtils::MakeMaker (perl 5.42.2 core) (perl 5.44.0 core)
# |     build_requires : ExtUtils::MakeMaker (perl 5.42.2 core) (perl 5.44.0 core)
# |           requires : HTTP::Headers

[PERL_542].USES_ON=			perl:542,configure
[PERL_542].BUILDRUN_DEPENDS_ON=		perl-HTTP-Message:single:542
[PERL_544].USES_ON=			perl:544,configure
[PERL_544].BUILDRUN_DEPENDS_ON=		perl-HTTP-Message:single:544

