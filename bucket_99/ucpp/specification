DEF[PORTVERSION]=	1.3.5
DEF[SOVERSION]=		13.0.5
DEF[COMMIT]=		79f08e39c676f15ed8a59335f6c9b924
# ----------------------------------------------------------------------------

NAMEBASE=		ucpp
VERSION=		${PORTVERSION}
KEYWORDS=		devel
VARIANTS=		standard
SDESC[standard]=	C preprocessor and lexer
HOMEPAGE=		https://gitlab.com/scarabeusiv/ucpp
CONTACT=		nobody

DOWNLOAD_GROUPS=	main
SITES[main]=		https://gitlab.com/scarabeusiv/ucpp/uploads/${COMMIT}/
DISTFILE[1]=		ucpp-${PORTVERSION}.tar.xz:main

SPKGS[standard]=	single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

LICENSE=		BSD3CLAUSE:single
LICENSE_FILE=		BSD3CLAUSE:{{WRKSRC}}/LICENSE
LICENSE_SCHEME=		solo

FPC_EQUIVALENT=		devel/ucpp

USES=			autoreconf gmake libtool
MUST_CONFIGURE=		gnu
INSTALL_TARGET=		install-strip
SOVERSION=		${SOVERSION}
