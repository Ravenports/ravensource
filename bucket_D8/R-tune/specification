DEF[PORTVERSION]=	2.0.1
# ------------------------------------------------------------------------

NAMEBASE=		R-tune
VERSION=		${PORTVERSION}
KEYWORDS=		cran
VARIANTS=		std
SDESC[std]=		Tidy Tuning Tools
HOMEPAGE=		https://tune.tidymodels.org/
CONTACT=		CRAN_Automaton[cran@ironwolf.systems]

DOWNLOAD_GROUPS=	main
SITES[main]=		CRAN/src/contrib
			https://loki.dragonflybsd.org/cranfiles/
DISTFILE[1]=		tune_2.0.1.tar.gz:main
DIST_SUBDIR=		CRAN
DF_INDEX=		1

SPKGS[std]=		single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

# License listed on https://cran.r-project.org/
# => MIT + file LICENSE

USES=			cran gmake
DISTNAME=		tune
GENERATED=		yes
INSTALL_REQ_TOOLCHAIN=	yes

BUILDRUN_DEPENDS=	R-cli:single:std
			R-dials:single:std
			R-dplyr:single:std
			R-generics:single:std
			R-ggplot2:single:std
			R-glue:single:std
			R-GPfit:single:std
			R-hardhat:single:std
			R-parsnip:single:std
			R-purrr:single:std
			R-recipes:single:std
			R-rlang:single:std
			R-rsample:single:std
			R-tailor:single:std
			R-tibble:single:std
			R-tidyr:single:std
			R-tidyselect:single:std
			R-vctrs:single:std
			R-withr:single:std
			R-workflows:single:std
			R-yardstick:single:std


