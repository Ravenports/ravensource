--- configure.orig	2024-11-18 20:33:54 UTC
+++ configure
@@ -12510,7 +12510,7 @@ if ac_fn_c_try_link "$LINENO"; then :
       # recognize dependent libraries; assume curses is needed if we didn't
       # find any dependency.
       case $host_os in
-        netbsd* | openbsd*)
+        xxxxxx*)
           if test x"$pgac_lib" = x"" ; then
             pgac_lib=" -lcurses"
           fi ;;
