--- configure.orig	2021-08-09 20:49:05 UTC
+++ configure
@@ -11904,7 +11904,7 @@ if ac_fn_c_try_link "$LINENO"; then :
       # recognize dependent libraries; assume curses is needed if we didn't
       # find any dependency.
       case $host_os in
-        netbsd* | openbsd*)
+        xxxxxx*)
           if test x"$pgac_lib" = x"" ; then
             pgac_lib=" -lcurses"
           fi ;;
