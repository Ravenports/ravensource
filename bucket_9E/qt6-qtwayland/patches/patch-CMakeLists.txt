--- CMakeLists.txt.orig	2023-05-16 09:46:14 UTC
+++ CMakeLists.txt
@@ -17,10 +17,6 @@ find_package(Qt6 ${PROJECT_VERSION} CONF
     BuildInternals
 )
 
-if(NOT MACOS AND NOT QNX AND (ANDROID OR NOT LINUX))
-    message(NOTICE "Skipping the build as the condition \"LINUX OR MACOS OR QNX\" is not met.")
-    return()
-endif()
 
 find_package(Qt6 ${PROJECT_VERSION} CONFIG REQUIRED COMPONENTS
     Core
