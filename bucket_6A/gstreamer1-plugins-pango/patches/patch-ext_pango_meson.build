--- ext/pango/meson.build.orig	2026-01-27 17:02:33 UTC
+++ ext/pango/meson.build
@@ -26,6 +26,7 @@ plugin_sources += {
   'pango': pathsep.join(doc_sources)
 }
 
+video_dep = cc.find_library ('gstvideo-1.0', required : true)
 pango_dep = dependency('pangocairo', version : '>=1.22.0', required : get_option('pango'),
   allow_fallback: true, default_options: ['cairo=enabled'])
 
