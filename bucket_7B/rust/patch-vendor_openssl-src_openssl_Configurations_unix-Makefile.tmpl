--- vendor/openssl-src/openssl/Configurations/unix-Makefile.tmpl.orig	2022-11-02 16:21:42 UTC
+++ vendor/openssl-src/openssl/Configurations/unix-Makefile.tmpl
@@ -982,7 +982,7 @@ libssl.pc:
 	    echo 'Description: Secure Sockets Layer and cryptography libraries'; \
 	    echo 'Version: '$(VERSION); \
 	    echo 'Requires.private: libcrypto'; \
-	    echo 'Libs: -L$${libdir} -lssl'; \
+	    echo 'Libs: -L$${libdir} -lssl_pic'; \
 	    echo 'Cflags: -I$${includedir}' ) > libssl.pc
 
 openssl.pc:
