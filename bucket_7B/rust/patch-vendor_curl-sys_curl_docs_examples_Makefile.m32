--- vendor/curl-sys/curl/docs/examples/Makefile.m32.orig	2022-11-02 16:21:41 UTC
+++ vendor/curl-sys/curl/docs/examples/Makefile.m32
@@ -292,7 +292,7 @@ ifdef SSL
     $(error Invalid path to OpenSSL package: $(OPENSSL_PATH))
   endif
   ifndef OPENSSL_LIBPATH
-    OPENSSL_LIBS = -lssl -lcrypto
+    OPENSSL_LIBS = -lssl_pic -lcrypto_pic
     ifeq "$(wildcard $(OPENSSL_PATH)/out)" "$(OPENSSL_PATH)/out"
       OPENSSL_LIBPATH = $(OPENSSL_PATH)/out
       ifdef DYN
