USES=			pkgconfig
BUILD_DEPENDS=		gdal:dev:std
BUILD_DEPENDS=		proj:dev:std
BUILD_DEPENDS=		sqlite:dev:std
BUILD_DEPENDS=		geos:dev:std

BUILDRUN_DEPENDS=	gdal:primary:std
BUILDRUN_DEPENDS=	proj:primary:std
BUILDRUN_DEPENDS=	sqlite:primary:std
BUILDRUN_DEPENDS=	geos:primary:std
