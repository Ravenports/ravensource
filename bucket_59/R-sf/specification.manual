USES=			pkgconfig sqlite tiff curl
BUILD_DEPENDS=		geos:dev:standard
BUILD_DEPENDS=		proj:dev:standard
BUILD_DEPENDS=		gdal:dev:standard
BUILDRUN_DEPENDS=	gdal:primary:standard
BUILDRUN_DEPENDS=	geos:primary:standard
