DEF[PORTVERSION]=	0.7.2
# ------------------------------------------------------------------------

NAMEBASE=		python-pyocr
VERSION=		${PORTVERSION}
KEYWORDS=		python graphics
VARIANTS=		py36 py37
SDESC[py36]=		Python wrapper for OCR engines (Tesseract, (PY 36)
SDESC[py37]=		Python wrapper for OCR engines (Tesseract, (PY 37)
HOMEPAGE=		https://gitlab.gnome.org/World/OpenPaperwork/pyocr
CONTACT=		Python_Automaton[python@ironwolf.systems]

DOWNLOAD_GROUPS=	main
SITES[main]=		PYPI/p/pyocr
DISTFILE[1]=		pyocr-0.7.2.tar.gz:main

SPKGS[py36]=		single
SPKGS[py37]=		single

OPTIONS_AVAILABLE=	PY36 PY37
OPTIONS_STANDARD=	none
VOPTS[py36]=		PY36=ON PY37=OFF
VOPTS[py37]=		PY36=OFF PY37=ON

# Output of setup.py --license
# GPLv3+

GENERATED=		yes

# install_requires extracted from setup.py (using python 2.7)

DISTNAME=		pyocr-${PORTVERSION}

[PY36].USES_ON=				python:py36
[PY36].BUILDRUN_DEPENDS_ON=		python-Pillow:single:py36
					python-setuptools_scm:single:py36
					python-setuptools_scm_git_archive:single:py36

[PY37].USES_ON=				python:py37
[PY37].BUILDRUN_DEPENDS_ON=		python-Pillow:single:py37
					python-setuptools_scm:single:py37
					python-setuptools_scm_git_archive:single:py37
RUN_DEPENDS=		tesseract:complete:standard
