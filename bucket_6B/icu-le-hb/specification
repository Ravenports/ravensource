DEF[PORTVERSION]=	1.0.3
# ----------------------------------------------------------------------------

NAMEBASE=		icu-le-hb
VERSION=		${PORTVERSION}
KEYWORDS=		devel print
VARIANTS=		standard
SDESC[standard]=	ICU Layout Engine API on top of HarfBuzz shaping
HOMEPAGE=		none
CONTACT=		nobody

DOWNLOAD_GROUPS=	main
SITES[main]=		https://www.freedesktop.org/software/harfbuzz/release/
DISTFILE[1]=		icu-le-hb-${PORTVERSION}.tar.gz:main

SPKGS[standard]=	single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

LICENSE=		MIT:single
LICENSE_FILE=		MIT:{{WRKSRC}}/COPYING
LICENSE_SCHEME=		solo

BUILDRUN_DEPENDS=	harfbuzz:single:standard

USES=			gmake libtool pkgconfig
MUST_CONFIGURE=		gnu
INSTALL_TARGET=		install-strip
