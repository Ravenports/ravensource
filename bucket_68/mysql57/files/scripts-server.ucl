post-deinstall-lua: [{
    args: ""
    code: <<EOS
dbdir = "/var/db/mysql"
if pkg_rootdir ~= "/" then
   dbdir = pkg_rootdir .. dbdir
end

xres = pkg.readdir(dbdir)
if #xres == 0 then
   os.remove(dbdir)
end
EOS
}]
