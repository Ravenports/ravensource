# manually updated
DEF[PORTVERSION]=	0.7201
DEF[TAG]=		${PORTVERSION}
# ------------------------------------------------------------------------

NAMEBASE=		perl-OpenGL-GLUT
VERSION=		${PORTVERSION}
REVISION=		1
KEYWORDS=		perl
VARIANTS=		542 544
SDESC[542]=		Perl bindings to the GLUT/FreeGLUT toolkit (5.42)
SDESC[544]=		Perl bindings to the GLUT/FreeGLUT toolkit (5.44)
HOMEPAGE=		https://github.com/Perl-GPU/OpenGL-GLUT
CONTACT=		Perl_Automaton[perl@ravenports.com]

DOWNLOAD_GROUPS=	main
# SITES[main]=		CPAN/ID:E/ET/ETJ
# DISTFILE[1]=		OpenGL-GLUT-${PORTVERSION}.tar.gz:main
SITES[main]=		GITHUB/Perl-GPU:OpenGL-GLUT:${TAG}
DISTFILE[1]=		generated:main

SPKGS[542]=		single
SPKGS[544]=		single

OPTIONS_AVAILABLE=	PERL_542 PERL_544
OPTIONS_STANDARD=	none
VOPTS[542]=		PERL_542=ON PERL_544=OFF
VOPTS[544]=		PERL_542=OFF PERL_544=ON

# License listed in CPAN directory:
# unknown

GENERATED=		yes
SINGLE_JOB=		yes
DISTNAME=		OpenGL-GLUT-${PORTVERSION}

# ----------------------------------------------
# |   Prerequisites extracted from META.json
# ----------------------------------------------
# |      runtime : OpenGL
# |        build : ExtUtils::MakeMaker (perl 5.38 core) (perl 5.40 core)
# |    configure : ExtUtils::MakeMaker (perl 5.38 core) (perl 5.40 core)
# |         test : Test::More (perl 5.38 core) (perl 5.40 core)

[PERL_542].USES_ON=			perl:542,configure
[PERL_542].BUILDRUN_DEPENDS_ON=		perl-OpenGL:single:542
[PERL_544].USES_ON=			perl:544,configure
[PERL_544].BUILDRUN_DEPENDS_ON=		perl-OpenGL:single:544

BUILD_DEPENDS=		freeglut:dev:std
CONFIGURE_ARGS=		verbose
XORG_COMPONENTS=	x11 xext xmu xi ice
VAR_OPSYS[linux]=	CFLAGS=-Doff64_t=__off64_t
