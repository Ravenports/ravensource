DEF[PORTVERSION]=	1.12
# ----------------------------------------------------------------------------

NAMEBASE=		xorg-sgml-doctools
VERSION=		${PORTVERSION}
KEYWORDS=		textproc x11
VARIANTS=		standard
SDESC[standard]=	Stylesheets and entities for X.Org documentation
HOMEPAGE=		https://gitlab.freedesktop.org/xorg/doc/xorg-sgml-doctools
CONTACT=		nobody

DOWNLOAD_GROUPS=	main
SITES[main]=		XORG/individual/doc
DISTFILE[1]=		xorg-sgml-doctools-${PORTVERSION}.tar.bz2:main
DIST_SUBDIR=		xorg

SPKGS[standard]=	single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

FPC_EQUIVALENT=		textproc/xorg-sgml-doctools

LICENSE=		MIT:single
LICENSE_FILE=		MIT:{{WRKSRC}}/COPYING
LICENSE_SCHEME=		solo

USES=			gmake
DISTNAME=		xorg-sgml-doctools-${PORTVERSION}
MUST_CONFIGURE=		gnu
MAKE_ARGS=		pkgconfigdir="{{PREFIX}}/lib/pkgconfig"
