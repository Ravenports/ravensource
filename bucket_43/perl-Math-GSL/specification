DEF[PORTVERSION]=	0.49
# ------------------------------------------------------------------------

NAMEBASE=		perl-Math-GSL
VERSION=		${PORTVERSION}
KEYWORDS=		perl
VARIANTS=		542 544
SDESC[542]=		Interface to the GNU Scientific Library (5.42)
SDESC[544]=		Interface to the GNU Scientific Library (5.44)
HOMEPAGE=		https://github.com/hakonhagland/perl-math-gsl/tree/master
CONTACT=		Perl_Automaton[perl@ravenports.com]

DOWNLOAD_GROUPS=	main
SITES[main]=		CPAN/ID:H/HA/HAKONH
DISTFILE[1]=		Math-GSL-${PORTVERSION}.tar.gz:main

SPKGS[542]=		single
SPKGS[544]=		single

OPTIONS_AVAILABLE=	PERL_542 PERL_544
OPTIONS_STANDARD=	none
VOPTS[542]=		PERL_542=ON PERL_544=OFF
VOPTS[544]=		PERL_542=OFF PERL_544=ON

# License listed in CPAN directory:
# gpl_1

GENERATED=		yes
SINGLE_JOB=		yes
DISTNAME=		Math-GSL-${PORTVERSION}

# ----------------------------------------------
# |   Prerequisites extracted from META.json
# ----------------------------------------------
# |      runtime : Math::Complex (perl 5.42 core) (perl 5.44 core)
# |      runtime : Scalar::Util (perl 5.42 core) (perl 5.44 core)
# |      runtime : parent (perl 5.42 core) (perl 5.44 core)
# |      runtime : version (perl 5.42 core) (perl 5.44 core)
# |        build : Test::Class
# |        build : Test::Exception
# |        build : Test::More (perl 5.42 core) (perl 5.44 core)
# |        build : Test::Most
# |        build : Test::Taint
# |    configure : Math::GSL::Alien
# |    configure : Module::Build
# |    configure : PkgConfig

[PERL_542].USES_ON=			perl:542,buildmod,run
[PERL_542].BUILD_DEPENDS_ON=		perl-Test-Class:single:542
					perl-Test-Exception:single:542
					perl-Test-Most:single:542
					perl-Test-Taint:single:542
					perl-Math-GSL-Alien:single:542
					perl-Module-Build:single:542
					perl-PkgConfig:single:542
[PERL_544].USES_ON=			perl:544,buildmod,run
[PERL_544].BUILD_DEPENDS_ON=		perl-Test-Class:single:544
					perl-Test-Exception:single:544
					perl-Test-Most:single:544
					perl-Test-Taint:single:544
					perl-Math-GSL-Alien:single:544
					perl-Module-Build:single:544
					perl-PkgConfig:single:544

USES=			cclibs:single
BUILD_DEPENDS=		gsl:dev:std
			gsl:tools:std
BUILDRUN_DEPENDS=	gsl:primary:std
			perl-Role-Tiny:single:perl_used
			perl-Alien-Role-Dino:single:perl_used
INSTALL_REQ_TOOLCHAIN=	yes

post-patch:
	${REINPLACE_CMD} -e 's|@EXT1@|-Wl,-rpath,${PREFIX}/lib|' \
		-e 's|@EXT2@|-Wl,-rpath,${PREFIX}/lib/perl5/${PERL_VER}/${PERL_ARCH}/CORE|' \
		${WRKSRC}/inc/GSLBuilder.pm
	# support GSL 2.7.1 (not needed since v0.44)
	# ${REINPLACE_CMD} -e 's|"2.7"|"2.7.1"|' ${WRKSRC}/inc/Ver2Func.pm
