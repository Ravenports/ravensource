DEF[PORTVERSION]=	20260402.0
# ------------------------------------------------------------------------

NAMEBASE=		perl-IPC-Run
VERSION=		${PORTVERSION}
KEYWORDS=		perl
VARIANTS=		542 544
SDESC[542]=		System() and background processes (5.42)
SDESC[544]=		System() and background processes (5.44)
HOMEPAGE=		https://github.com/cpan-authors/IPC-Run
CONTACT=		Perl_Automaton[perl@ravenports.com]

DOWNLOAD_GROUPS=	main
SITES[main]=		CPAN/ID:T/TO/TODDR
DISTFILE[1]=		IPC-Run-${PORTVERSION}.tar.gz:main

SPKGS[542]=		single
SPKGS[544]=		single

OPTIONS_AVAILABLE=	PERL_542 PERL_544
OPTIONS_STANDARD=	none
VOPTS[542]=		PERL_542=ON PERL_544=OFF
VOPTS[544]=		PERL_542=OFF PERL_544=ON

# License listed in CPAN directory:
# perl_5

GENERATED=		yes
SINGLE_JOB=		yes
DISTNAME=		IPC-Run-${PORTVERSION}

# ----------------------------------------------
# |   Prerequisites extracted from META.json
# ----------------------------------------------
# |      runtime : IO::Pty
# |      runtime : Test::More (perl 5.42 core) (perl 5.44 core)
# |      runtime : IO::Pty
# |      runtime : Readonly
# |        build : ExtUtils::MakeMaker (perl 5.42 core) (perl 5.44 core)
# |        build : Readonly::Array
# |        build : Test::More (perl 5.42 core) (perl 5.44 core)
# |    configure : ExtUtils::MakeMaker (perl 5.42 core) (perl 5.44 core)

[PERL_542].USES_ON=			perl:542,configure
[PERL_542].BUILDRUN_DEPENDS_ON=		perl-IO-Tty:single:542
					perl-Readonly:single:542
[PERL_544].USES_ON=			perl:544,configure
[PERL_544].BUILDRUN_DEPENDS_ON=		perl-IO-Tty:single:544
					perl-Readonly:single:544

