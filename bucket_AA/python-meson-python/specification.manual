[PY312].BUILDRUN_DEPENDS_ON=		meson:single:std
[PY314].BUILDRUN_DEPENDS_ON=		meson:single:v14

R_DEPS[linux]=		patchelf:primary:std

post-install:
	${SED} -i'' -e "s|sys.platform == 'linux':|True:|" \
		${STAGEDIR}${PYTHON_SITELIBDIR}/mesonpy/_rpath.py
