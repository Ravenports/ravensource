DEF[PORTVERSION]=	0.006022
# ------------------------------------------------------------------------

NAMEBASE=		perl-Devel-Declare
VERSION=		${PORTVERSION}
KEYWORDS=		perl
VARIANTS=		540 542
SDESC[540]=		Deprecated - Adds keywords to perl in perl (5.40)
SDESC[542]=		Deprecated - Adds keywords to perl in perl (5.42)
HOMEPAGE=		https://github.com/p5sagit/Devel-Declare
CONTACT=		Perl_Automaton[perl@ironwolf.systems]

DOWNLOAD_GROUPS=	main
SITES[main]=		CPAN/ID:E/ET/ETHER
DISTFILE[1]=		Devel-Declare-${PORTVERSION}.tar.gz:main

SPKGS[540]=		single
SPKGS[542]=		single

OPTIONS_AVAILABLE=	PERL_540 PERL_542
OPTIONS_STANDARD=	none
VOPTS[540]=		PERL_540=ON PERL_542=OFF
VOPTS[542]=		PERL_540=OFF PERL_542=ON

# License listed in CPAN directory:
# perl_5

GENERATED=		yes
SINGLE_JOB=		yes
DISTNAME=		Devel-Declare-${PORTVERSION}

# ----------------------------------------------
# |   Prerequisites extracted from META.json
# ----------------------------------------------
# |      runtime : B::Hooks::EndOfScope
# |      runtime : B::Hooks::OP::Check
# |      runtime : Scalar::Util (perl 5.40 core) (perl 5.42 core)
# |      runtime : Sub::Name
# |        build : B::Hooks::OP::Check
# |        build : Test::More (perl 5.40 core) (perl 5.42 core)
# |        build : Test::Requires
# |    configure : B::Hooks::OP::Check
# |    configure : ExtUtils::Depends

[PERL_540].USES_ON=			perl:540,configure
[PERL_540].BUILDRUN_DEPENDS_ON=		perl-B-Hooks-EndOfScope:single:540
					perl-B-Hooks-OP-Check:single:540
					perl-Sub-Name:single:540
[PERL_540].BUILD_DEPENDS_ON=		perl-Test-Requires:single:540
					perl-ExtUtils-Depends:single:540
[PERL_542].USES_ON=			perl:542,configure
[PERL_542].BUILDRUN_DEPENDS_ON=		perl-B-Hooks-EndOfScope:single:542
					perl-B-Hooks-OP-Check:single:542
					perl-Sub-Name:single:542
[PERL_542].BUILD_DEPENDS_ON=		perl-Test-Requires:single:542
					perl-ExtUtils-Depends:single:542

