DEF[PORTVERSION]=	4.50.0
# ------------------------------------------------------------------------

NAMEBASE=		python-tqdm
VERSION=		${PORTVERSION}
KEYWORDS=		python
VARIANTS=		py38 py37
SDESC[py38]=		Fast, Extensible Progress Meter (PY38)
SDESC[py37]=		Fast, Extensible Progress Meter (PY37)
HOMEPAGE=		https://github.com/tqdm/tqdm
CONTACT=		Python_Automaton[python@ironwolf.systems]

DOWNLOAD_GROUPS=	main
SITES[main]=		PYPI/t/tqdm
DISTFILE[1]=		tqdm-${PORTVERSION}.tar.gz:main

SPKGS[py38]=		single
SPKGS[py37]=		single

OPTIONS_AVAILABLE=	PY38 PY37
OPTIONS_STANDARD=	none
VOPTS[py38]=		PY38=ON PY37=OFF
VOPTS[py37]=		PY38=OFF PY37=ON

# License listed at PyPI
# MPLv2.0, MIT Licences

# install_requires extracted from setup.py


DISTNAME=		tqdm-${PORTVERSION}
GENERATED=		yes

[PY38].USES_ON=				python:py38
[PY37].USES_ON=				python:py37

