--- configure.orig	2025-11-24 23:10:29 UTC
+++ configure
@@ -9089,11 +9089,9 @@ case "${host_os}" in
     SYS=bsdi
     CFLAGS="${CFLAGS} -pthread"
     ;;
-  freebsd*)
+  freebsd*|midnight*)
     SYS=freebsd
     CFLAGS="${CFLAGS} -pthread"
-    CPPFLAGS="${CPPFLAGS} -I/usr/local/include"
-    LDFLAGS="${LDFLAGS} -L/usr/local/lib"
     ac_cv_ld_z_defs=no
     ;;
   openbsd*)
@@ -9106,6 +9104,7 @@ case "${host_os}" in
     ;;
   dragonfly*)
     SYS=dragonfly
+    ac_cv_ld_z_defs=no
     ;;
   darwin*)
         CFLAGS="${CFLAGS} -fno-stack-check"
