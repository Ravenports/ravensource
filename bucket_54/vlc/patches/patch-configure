--- configure.orig	2025-12-23 10:59:11 UTC
+++ configure
@@ -9097,11 +9097,9 @@ case "${host_os}" in
     SYS=bsdi
     CFLAGS="${CFLAGS} -pthread"
     ;;
-  freebsd*)
+  freebsd*|midnight*)
     SYS=freebsd
     CFLAGS="${CFLAGS} -pthread"
-    CPPFLAGS="${CPPFLAGS} -I/usr/local/include"
-    LDFLAGS="${LDFLAGS} -L/usr/local/lib"
     ac_cv_ld_z_defs=no
     ;;
   openbsd*)
@@ -9114,6 +9112,7 @@ case "${host_os}" in
     ;;
   dragonfly*)
     SYS=dragonfly
+    ac_cv_ld_z_defs=no
     ;;
   darwin*)
         CFLAGS="${CFLAGS} -fno-stack-check"
