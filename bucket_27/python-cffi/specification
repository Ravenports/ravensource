DEF[PORTVERSION]=	2.0.0
# ------------------------------------------------------------------------

NAMEBASE=		python-cffi
VERSION=		${PORTVERSION}
KEYWORDS=		python
VARIANTS=		v13 v14
SDESC[v13]=		Foreign Function Interface, C (3.13)
SDESC[v14]=		Foreign Function Interface, C (3.14)
HOMEPAGE=		https://pypi.org/project/cffi/
CONTACT=		Python_Automaton[python@ironwolf.systems]

DOWNLOAD_GROUPS=	main
SITES[main]=		PYPI/c/cffi
DISTFILE[1]=		cffi-${PORTVERSION}.tar.gz:main
DIST_SUBDIR=		python-src

SPKGS[v13]=		single
SPKGS[v14]=		single

OPTIONS_AVAILABLE=	PY313 PY314
OPTIONS_STANDARD=	none
VOPTS[v13]=		PY313=ON PY314=OFF
VOPTS[v14]=		PY313=OFF PY314=ON

# License listed at PyPI
# Not provided.

GENERATED=		yes
DISTNAME=		cffi-${PORTVERSION}

# Requires-Dist extracted from pyproject.toml file
# buildrun: pycparser; implementation_name != 'PyPy'

[PY313].USES_ON=			python:v13,pep517
[PY313].BUILDRUN_DEPENDS_ON=		python-pycparser:single:v13
[PY314].USES_ON=			python:v14,pep517
[PY314].BUILDRUN_DEPENDS_ON=		python-pycparser:single:v14

INSTALL_REQ_TOOLCHAIN=	yes
BUILD_DEPENDS=		libffi:dev:std
RUN_DEPENDS=		python-pycparser:single:python_used
