DEF[PORTVERSION]=	1.0.2
# ----------------------------------------------------------------------------

NAMEBASE=		menu-cache
VERSION=		${PORTVERSION}
KEYWORDS=		x11
VARIANTS=		standard
SDESC[standard]=	Caching mechanism for freedesktop-compliant menus
HOMEPAGE=		http://lxde.org/
CONTACT=		nobody

DOWNLOAD_GROUPS=	main
SITES[main]=		SF/lxde/menu-cache/${PORTVERSION:R}/
DISTFILE[1]=		menu-cache-${PORTVERSION}.tar.xz:main

SPKGS[standard]=	single

OPTIONS_AVAILABLE=	none
OPTIONS_STANDARD=	none

FPC_EQUIVALENT=		x11/menu-cache

BUILDRUN_DEPENDS=	libfm-extra:single:standard

USES=			gmake gettext-runtime libtool pkgconfig
GNOME_COMPONENTS=	glib
INSTALL_TARGET=		install-strip
MUST_CONFIGURE=		gnu
CONFIGURE_ARGS=		--disable-static
