# manually maintained (pypi didn't publish source files)
DEF[PORTVERSION]=	9.6.0
DEF[TAG]=		${PORTVERSION}
# ------------------------------------------------------------------------

NAMEBASE=		python-mysql-connector-python
VERSION=		${PORTVERSION}
KEYWORDS=		python
VARIANTS=		v13 v14
SDESC[v13]=		MySQL server driver (3.13)
SDESC[v14]=		MySQL server driver (3.14)
HOMEPAGE=		https://dev.mysql.com/doc/connector-python/en/
CONTACT=		Python_Automaton[python@ironwolf.systems]

DOWNLOAD_GROUPS=	main
# SITES[main]=		PYPI/m/mysql-connector-python
# DISTFILE[1]=		mysql_connector_python-${PORTVERSION}.tar.gz:main
SITES[main]=		GITHUB/mysql:mysql-connector-python:${TAG}
DISTFILE[1]=		generated:main
DIST_SUBDIR=		python-src

SPKGS[v13]=		single
SPKGS[v14]=		single

OPTIONS_AVAILABLE=	PY313 PY314
OPTIONS_STANDARD=	none
VOPTS[v13]=		PY313=ON PY314=OFF
VOPTS[v14]=		PY313=OFF PY314=ON

# License listed at PyPI
# GNU GPLv2 (with FOSS License Exception)

GENERATED=		yes
# DISTNAME=		mysql_connector_python-${PORTVERSION}

# Requires-Dist extracted from pyproject.toml file


[PY313].USES_ON=			python:v13,pep517
[PY314].USES_ON=			python:v14,pep517

