--- lib/configure.orig	2025-06-21 16:06:20 UTC
+++ lib/configure
@@ -31,6 +31,10 @@ if [ -z "$HOST" ] ; then
 	then
 		sys=freebsd
 	fi
+	if [ "$sys" = "MidnightBSD" ]
+	then
+		sys=freebsd
+	fi
 	if [ "$sys" = "GNU/kFreeBSD" ]
 	then
 		sys=kfreebsd
