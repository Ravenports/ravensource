This module implements the same algorithm as
Algorithm::RectanglesContainingDot in C/XS and so it is much faster (around
30 times faster!). The Algorithm::RectanglesContainingDot module will use
this implementation automatically when available.
