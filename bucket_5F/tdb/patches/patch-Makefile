--- Makefile.orig	2019-01-14 22:24:45 UTC
+++ Makefile
@@ -1,6 +1,6 @@
 # simple makefile wrapper to run waf
 
-WAF_BINARY=$(PYTHON) ../../buildtools/bin/waf
+WAF_BINARY=$(PYTHON) buildtools/bin/waf
 WAF=PYTHONHASHSEED=1 WAF_MAKE=1 $(WAF_BINARY)
 
 all:
