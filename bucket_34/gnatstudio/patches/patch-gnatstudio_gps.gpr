--- gnatstudio/gps.gpr.orig	2021-06-15 05:19:41 UTC
+++ gnatstudio/gps.gpr
@@ -80,6 +80,7 @@ project GPS is
 
       --  for Switches ("Ada") use ("-lgmem");
       --  for Switches ("Ada") use ("-pg");
+      for Linker_Options use ("-Wl,-rpath,@PREFIX@/lib");
    end Linker;
 
    package CodePeer is
