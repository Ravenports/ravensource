--- CMakeLists.txt.orig	2026-02-20 00:30:46 UTC
+++ CMakeLists.txt
@@ -837,10 +837,6 @@ foreach(P cli daemon gtk mac qt utils)
     endif()
 endforeach()
 
-if(ENABLE_DAEMON OR ENABLE_GTK OR ENABLE_QT)
-    tr_install_web(${CMAKE_INSTALL_DATAROOTDIR}/${TR_NAME})
-endif()
-
 if(INSTALL_DOC)
     install(
         FILES
