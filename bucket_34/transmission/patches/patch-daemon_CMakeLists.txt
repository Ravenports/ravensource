--- daemon/CMakeLists.txt.orig	2023-02-22 19:12:15 UTC
+++ daemon/CMakeLists.txt
@@ -30,6 +30,9 @@ target_include_directories(${TR_NAME}-da
     PRIVATE
         $<$<BOOL:${WITH_SYSTEMD}>:${SYSTEMD_INCLUDE_DIRS}>)
 
+if (CMAKE_SYSTEM_NAME MATCHES "NetBSD")
+   target_link_libraries(${TR_NAME}-daemon quota)
+endif()
 target_link_libraries(${TR_NAME}-daemon
     PRIVATE
         ${TR_NAME}
