--- cli/CMakeLists.txt.orig	2023-04-14 02:22:25 UTC
+++ cli/CMakeLists.txt
@@ -4,6 +4,9 @@ target_sources(${TR_NAME}-cli
     PRIVATE
         cli.cc)
 
+if (CMAKE_SYSTEM_NAME MATCHES "NetBSD")
+   target_link_libraries(${TR_NAME}-cli quota)
+endif()
 target_link_libraries(${TR_NAME}-cli
     PRIVATE
         ${TR_NAME}
