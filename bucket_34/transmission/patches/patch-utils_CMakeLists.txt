--- utils/CMakeLists.txt.orig	2023-04-14 02:22:25 UTC
+++ utils/CMakeLists.txt
@@ -5,6 +5,9 @@ foreach(P create edit remote show)
         PRIVATE
             ${P}.cc)
 
+    if (CMAKE_SYSTEM_NAME MATCHES "NetBSD")
+       target_link_libraries(${TR_NAME}-${P} quota)
+    endif()
     target_link_libraries(${TR_NAME}-${P}
         PRIVATE
             ${TR_NAME}
