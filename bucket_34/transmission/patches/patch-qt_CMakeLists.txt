--- qt/CMakeLists.txt.orig	2023-04-14 02:22:25 UTC
+++ qt/CMakeLists.txt
@@ -204,6 +204,9 @@ target_include_directories(${TR_NAME}-qt
     PRIVATE
         ${CMAKE_CURRENT_SOURCE_DIR})
 
+if (CMAKE_SYSTEM_NAME MATCHES "NetBSD")
+   target_link_libraries(${TR_NAME}-qt quota)
+endif()
 target_link_libraries(${TR_NAME}-qt
     PRIVATE
         ${TR_NAME}
