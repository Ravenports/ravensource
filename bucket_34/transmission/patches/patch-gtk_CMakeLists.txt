--- gtk/CMakeLists.txt.orig	2023-04-14 02:22:25 UTC
+++ gtk/CMakeLists.txt
@@ -189,6 +189,9 @@ target_include_directories(${TR_NAME}-gt
     PRIVATE
         $<$<BOOL:${WITH_APPINDICATOR}>:${APPINDICATOR_INCLUDE_DIRS}>)
 
+if (CMAKE_SYSTEM_NAME MATCHES "NetBSD")
+   target_link_libraries(${TR_NAME}-gtk quota)
+endif()
 target_link_libraries(${TR_NAME}-gtk
     PRIVATE
         ${TR_NAME}
