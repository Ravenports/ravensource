--- meson.build.orig	2022-02-02 19:12:58 UTC
+++ meson.build
@@ -80,6 +80,7 @@ if os_backend == 'linux'
     endif
     cdata.set10('HAVE_IDEVICE', true)
   endif
+  subdir('rules')
 endif
 
 historydir = get_option('historydir')
@@ -104,7 +105,6 @@ endif
 config_h = configure_file(output: 'config.h', configuration: cdata)
 
 subdir('etc')
-subdir('rules')
 subdir('po')
 subdir('dbus')
 subdir('libupower-glib')
