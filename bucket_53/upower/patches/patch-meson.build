--- meson.build.orig	2022-02-23 16:46:51 UTC
+++ meson.build
@@ -104,8 +104,10 @@ endif
 # Generate configuration file
 config_h = configure_file(output: 'config.h', configuration: cdata)
 
+if os_backend == 'linux'
+  subdir('rules')
+endif
 subdir('etc')
-subdir('rules')
 subdir('po')
 subdir('dbus')
 subdir('libupower-glib')
