--- meson.build.orig	2022-02-09 16:53:54 UTC
+++ meson.build
@@ -81,6 +81,7 @@ if os_backend == 'linux'
     endif
     cdata.set10('HAVE_IDEVICE', true)
   endif
+  subdir('rules')
 endif
 
 historydir = get_option('historydir')
@@ -105,7 +106,6 @@ endif
 config_h = configure_file(output: 'config.h', configuration: cdata)
 
 subdir('etc')
-subdir('rules')
 subdir('po')
 subdir('dbus')
 subdir('libupower-glib')
