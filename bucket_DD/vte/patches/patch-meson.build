--- meson.build.orig	2023-09-12 21:06:50 UTC
+++ meson.build
@@ -237,6 +237,10 @@ elif system == 'darwin'
     ['_DARWIN_C_SOURCE', '1'],
   ]
 
+elif system == 'dragonfly'
+  libc_feature_defines = []
+elif system == 'midnightbsd'
+  libc_feature_defines = []
 elif system == 'netbsd'
   libc_feature_defines += [
     ['_NETBSD_SOURCE', '1'],
