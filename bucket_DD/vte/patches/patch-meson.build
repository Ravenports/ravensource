--- meson.build.orig	2022-09-03 16:50:59 UTC
+++ meson.build
@@ -229,6 +229,8 @@ if system == 'freebsd'
 
   libc_feature_defines = []
 
+elif system == 'dragonfly'
+  libc_feature_defines = []
 elif system == 'netbsd'
   libc_feature_defines += [
     '_NETBSD_SOURCE',
