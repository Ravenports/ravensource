--- tf-psa-crypto/drivers/p256-m/CMakeLists.txt.orig	2026-03-27 10:33:07 UTC
+++ tf-psa-crypto/drivers/p256-m/CMakeLists.txt
@@ -26,6 +26,7 @@ foreach (target IN LISTS target_librarie
               ${TF_PSA_CRYPTO_DRIVERS_INCLUDE_DIRS}
     )
     tf_psa_crypto_set_config_files_compile_definitions(${target})
+    set_property(TARGET ${p256m_static_target} PROPERTY POSITION_INDEPENDENT_CODE ON)
     if(TF_PSA_CRYPTO_TEST_DRIVER)
         add_dependencies(${target} ${TF_PSA_CRYPTO_TEST_DRIVER_GENERATION_TARGETS})
     endif()
